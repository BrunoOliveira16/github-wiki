import styled from "styled-components";

export const ItemContainer = styled.div `
    width: 80%;

    h3 {
        font-size: 2rem;
        color: #fafafa;
    }

    p {
        font-size: 1rem;
        color: #fafafa60;
        margin-bottom: 1.25rem;
    }

    a.remover {
        color: #ff0000;
    }

    hr {
        color: #fafafa60;
        margin: 1.25rem 0;
    }
`